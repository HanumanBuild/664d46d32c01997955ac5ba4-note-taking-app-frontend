// Ensure TailwindCSS is installed and configured
module.exports = {
  // Add content paths for Tailwind to scan
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
}